export default function HomePage() {
  return (
    <main className="app-shell">
      <section className="hero">
        <p className="eyebrow">WIAL Platform</p>
        <h1>Backend is running.</h1>
        <p className="lede">
          The application shell is live, and the API routes can now be tested
          locally while the rest of the pages are being built.
        </p>
      </section>
    </main>
  );
}
